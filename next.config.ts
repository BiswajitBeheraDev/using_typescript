// next.config.ts

import type { NextConfig } from 'next';

const config: NextConfig = {
  images: {
    domains: ['plus.unsplash.com', 'images.unsplash.com'], // Add the domains here
  },
};

export default config;
